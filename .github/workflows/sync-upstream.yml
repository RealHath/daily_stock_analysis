name: Sync Upstream

on:
  schedule:
    # UTC+8 17:00 = UTC 09:00
    # UTC+8 17:00 = UTC 09:00, 仅工作日（周一至周五）
    - cron: '0 9 * * 1-5'
  workflow_dispatch:  # 支持手动触发

permissions:
  contents: write
  workflows: write  # 允许推送对 .github/workflows/ 下文件的变更

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/ZhuLinsen/daily_stock_analysis.git
          git fetch upstream

      - name: Check for upstream changes
        run: |
          behind=$(git rev-list --count main..upstream/main)
          echo "behind=$behind" >> $GITHUB_ENV
          echo "当前分支落后上游 $behind 个提交"

      - name: Merge upstream/main
        if: env.behind != '0'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git merge upstream/main --no-edit

      - name: Push to origin
        if: env.behind != '0'
        run: |
          git push origin main
